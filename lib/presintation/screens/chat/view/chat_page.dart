import 'package:flutter/widgets.dart';

class ChatPage extends StatelessWidget {
  const ChatPage({super.key, required String chatId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}