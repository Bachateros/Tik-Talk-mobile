import 'package:flutter/widgets.dart';

class ChatSettingsPage extends StatelessWidget {
  const ChatSettingsPage({super.key, required String chatId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}