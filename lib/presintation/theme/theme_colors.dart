import 'package:flutter/material.dart';

class AppColors {
  // Основные цвета
  static const primary = Color(0xFFAE7AFF);
  static const accent = Color(0xFF8A50FF);

  // Светлая тема
  static const lightBackground = Colors.white;
  static const lightText = Colors.black;
  static const lightTextStroke = Colors.black87;

  // Тёмная тема
  static const darkBackground = Colors.black;
  static const darkText = Colors.white;
  static const chatConteiner = Color.fromRGBO(28, 28, 28, 1);
  static const menuGrey = Colors.white70;
}
